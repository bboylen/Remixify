import React, { useState, useEffect } from 'react';

const App:React.FC = () => {
  const [data, setData] = useState(null);

  return (
    <div className="Login">
    </div>
  );
}

export default Login;
