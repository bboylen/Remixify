import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

export const Listen: React.FC = () => {
return(
  <Title className="listen" style={{
    marginTop: '5rem'
  }}>
    Coming Soon!
  </Title>
)
}